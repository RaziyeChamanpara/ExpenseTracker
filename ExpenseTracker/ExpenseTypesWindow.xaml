<expensetracker:BaseListWindow 
        x:Class="ExpenseTracker.ExpenseTypesWindow"
        x:TypeArguments="dataaccess:ExpenseType,expensetracker:AddEditExpenseTypeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:expensetrackercomponents="clr-namespace:ExpenseTrackerComponents;assembly=ExpenseTrackerComponents"
        xmlns:expensetracker="clr-namespace:ExpenseTracker"
        xmlns:dataaccess="clr-namespace:DataAccess;assembly=DataAccess"  
        mc:Ignorable="d"
                    
        Loaded="Window_Loaded"
        Title="ExpenseTypesWindow" Height="450" Width="800">
    
    <Grid Margin="50">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" 
                  Name="expenseTypesDataGrid"
                  ItemsSource="{Binding}" 
                  AlternatingRowBackground="LightSalmon"
                  AlternationCount="2"
                  AutoGenerateColumns="False" 
                  SelectedCellsChanged="ExpenseTypesDataGrid_SelectedCellsChanged">

            <DataGrid.Columns >
                <DataGridTextColumn Header="Name" Width="*" Binding="{Binding Name}"/>
            </DataGrid.Columns>

        </DataGrid>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal">

            <expensetrackercomponents:ExpenseTypesButton Content="Add" 
                                   Click="AddButton_Click"/>

            <expensetrackercomponents:ExpenseTypesButton Content="First" 
                                   Click="FirstButton_Click"/>

            <expensetrackercomponents:ExpenseTypesButton Content="Next" 
                                   Click="NextButton_Click"/>

            <expensetrackercomponents:ExpenseTypesButton Content="Previous" 
                                    Click="PreviousButton_Click"/>

            <expensetrackercomponents:ExpenseTypesButton Content="Last" 
                                    Click="LastButton_Click"/>

            <expensetrackercomponents:ExpenseTypesButton Content="Delete" 
                      Click="DeleteButton_Click"/>

            <expensetrackercomponents:ExpenseTypesButton Content="Edit"
                                Click="EditButton_Click"/>
            
        </StackPanel>
        
    </Grid>
    
</expensetracker:BaseListWindow>
